# PyQt Random Timer

Simple random timer GUI built with PyQt6.

## Installation

Clone repository

git clone https://github.com/GhostDainy/pyqt-random-timer.git

Enter project

cd pyqt-random-timer

Install dependencies

pip install -r requirements.txt

Run application

python main_timer.py